

GLboolean RivaSetBuffer ( GLcontext *ctx, GLenum mode );
GLbitfield RivaClearBuffers05(
    GLcontext *ctx,
    GLbitfield mask,
    GLboolean all,
    GLint x,
    GLint y,
    GLint width,
    GLint height);
